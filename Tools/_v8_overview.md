# 附魔音符换皮 v8 —— 逐类型对齐复制（修正 v7）

## 你提的 4 个问题，v8 现已全部满足
v8 改用具：**base 与样本同尺寸对齐 → 按位置复制样本色 + 用 base 的 alpha 当遮罩**。
因为 31 个音符类型的 base 与 003 样本全部同尺寸对齐，生成的技能3皮肤与美术师样本**逐像素一致（平均色差 = 0.0）**。

| 你的反馈 | 状态 | 说明 |
|---|---|---|
| ① 颜色还是偏暗 / 不接近样本 | ✅ 已修复 | 生成图 ≡ 技能3样本，亮度、色相完全贴合 |
| ② 爪印形状发生变化 | ✅ 已修复 | 只重着色不改几何，爪印形状 = 样本形状 |
| ③ 生成的爪印颜色 vs 样本差很多 | ✅ 已修复 | 爪色直接取自样本（如 Tap 爪 = (240,185,116)） |
| ④ Wide 外圈应是更深的圈 | ⚠️ 见下方待决 | 技能3样本里 Wide 外圈是**中棕(145,108,60)**，非深棕 |

## 比 v7 更关键的根因修正
v7 用"全局 奶油身+深棕圈"套所有类型 → 破坏了技能3**逐类独立配色**：
- Tap = 深棕圈+奶油身+金爪
- Wide / Tap_Small / Repeat / Slide_Link = **中棕身(145,108,60)+金黄点缀(223,175,60)**
- Repeat_i（带编号）= **亮青柠身(240,255,158)+亮黄**
- Slide_Judgment = 亮黄

v8 逐类对齐复制，以上全部精确还原（v7 把 Wide 染成奶油、Repeat_i 染成棕/奶油，全错）。

## 待你拍板：Wide 外圈颜色
你明确说过"想要像 004 那样更深的圈"，但**技能3样本本身**的 Wide 外圈就是中棕(145,108,60)，不是深棕。
- 若选"严格还原样本" → Wide = 中棕圈（v8 当前即此）。
- 若选"加深成 004 风格" → 我把 Wide（及可能的跨轨/胶囊类）外圈改深棕。

## 产物
- 生成集：`Tools/_gen_v8/`（31 张，命名 `Note_*_skill_3_howl.png`）
- 对比图：`Tools/_cmp_v8/montage_base_v8_004.png`（基础 | v8 | 004）
- 生成器：`Tools/enchant_skin_v8.py`（位置复制为主，桶兜底为辅）
- 验证脚本：`Tools/_probe_v7.py` / `_probe_paw.py` / `_montage_v8.py`

## 下一步（等你确认后）
确认方向 → 把 `_gen_v8/*` 覆盖进 `003/`（先备份当前 003 为可回退目录，2 张样本不动）→ 清理临时 `_gen_v7/_cmp_v7/_diag_v7` 等。
