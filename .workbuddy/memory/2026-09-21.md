# 2026-09-21 工作日志（切回副机 · 状态巡检）

## 工程实际位置与同步
- 本机工程路径：`D:\UGit\Musical-Sprite`（副机）。`D:\unity\plan go\Musical Sprite` 在本机**不存在**（D:\unity 只有 Unity 安装目录），交接文档里「以此为准」的路径注记与本机不符，需更正。
- `git rev-list --left-right --count origin/master...HEAD` = `0 0` → 本地 master 与 origin 完全同步，HEAD = `292ef4c`。
- 未提交改动：`M Assets/Art/Materials/M_GrassFringe.mat`、`M Assets/Art/Materials/M_GroundEdge_Arena.mat`、`?? Assets/Art/Characters/Shit/shit_export/shit_SkeletonData.asset(+.meta)`。

## 39 报错在本机的解决方式（与交接文档不同）
- 本机历史里破坏提交是 `4a930ca`（「2333」），**没有** revert 提交；恢复是靠 `2b3ff6e`（「ai实装」）重新加回运行时脚本，并删掉 `GroundEdge_Template.png.bak` / `SampleScene.unity.bak.*`。
- 故交接文档「五、本次会话修复」引用的 `fce3e96`/`06e7d2c`/`86f879e` 在本机历史中**不存在**，文档该段对本机已失效（架构/铁律/路线图部分仍有效）。

## 相对交接文档，项目已往前落地的模块
- **M2 Toon Ramp**：新增 `Assets/Shaders/ToonDoodle.shader`——完整卡通着色（_StepCount 色阶 / _ShadowThreshold / _ShadowColor / _HighlightThreshold / _HighlightColor）+ 描边(_OutlineWidth/_OutlineColor) + Doodle 抖动；URP Lit 改造，`UniversalMaterialType=Lit`。
- 其他新 shader：`GrassFringe.shader`、`HPBarFlash.shader`、`ScenePropSprite.shader`、`Assets/Art/UI/Shaders/AdditiveSprite.shader`。
- **能量槽 UI**：`203f8a7` 加入 EnergyBar 系列贴图 + AdditiveSprite。
- **AI 对手**：`2b3ff6e` 加 `Assets/Data/AI/OpponentAIProfile_{Easy,Medium,Hard,Nightmare}.asset` + `AICreateProfiles.cs` + `ShitSpineSetup.cs`，改 `ActiveSkillRuntime.cs`/`SampleScene.unity`。
- **新音符**：Repeat 音符类型（Note_Repeat 系列贴图）。
- **M4 基础**：早期提交 `12f8167/4f8aae3/75e239a/b99963d` 已有 `TerrainProfileApplier`、台面离地抬升 4 倍、地形 Apply 菜单。
- **角色导入**：屎屎(Shit) Spine 骨骼 `shit_SkeletonData.asset` 正在导入（未跟踪）。
- `b55b817`「修复草边黑线问题」已 commit（呼应 M0 edgeAO 黑线根因）。

## 本次提交（18:18）
- 用户确认只存屎屎、材质先放。已 `git add` 并 commit `0ae95fd`：**仅** `Assets/Art/Characters/Shit/shit_export/shit_SkeletonData.asset(+.meta)`，补全角色导入链。
- 两材质 `_CenterLineX` 微调保持未提交（按用户「以后再说」）。
- 环境注：本机 git-lfs/3.5.1 可用，pre-push hook 链式 LFS 可跑；.asset 非 LFS 文件。

## 待办（基于当前实际状态修正）
1. 继续/收尾 `ToonDoodle.shader` 卡通着色与描边（M2 主体）。
2. ~~提交未跟踪的屎屎 SkeletonData~~ → 已 commit `0ae95fd`（待 push 到远程才跨设备可见）。
3. 把未提交的 M_GrassFringe.mat / M_GroundEdge_Arena.mat 改动提交或回退（用户：以后再说）。
4. 校正交接文档路径注记 + 失效的 git SHA 段。
5. 雾效仍由用户自做 Shader Graph（Fog.shadergraph 在库）。
