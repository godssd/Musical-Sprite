# 附魔规则（技能皮肤实装 · 统一依据）

> 本文件是附魔机制的**权威规则手册**：定义附魔名额如何按音符类型分解、附魔皮肤如何套用、逐节点 / 逐数字恢复行为。
> 配套资产生成见同目录 `RECOLOR_RULES.md`（每皮肤 31 张贴图的批量生成器与锚点）。
> 最后对齐：2026-09-20 与用户讨论确认（链接音符按"链接带归属下个音节"逐段、连点音符按"每个数字 1 次附魔"）。

---

## 0. 范围与视觉载体

- **视觉载体 = 换皮**：被附魔的音符**整体换成对应技能的主题皮肤贴图**（非发光染色叠加）。
- **特效（VFX）在换皮生效后再单独追加**（本轮不做）。
- 附魔皮肤集 = 每技能 31 张（清单与生成见 `RECOLOR_RULES.md` 第 1 节）。

---

## 1. 基本概念

- **附魔名额（charm quota）** = 技能 `charmedNoteCount` + 过热加成（普通/过热 ≤ 6，超级过热 ≤ 10）。
  由 `NoteSpawner.RequestCharm` 登记，`AssignPendingCharms` / `ReserveUpcomingCharms` 按时间就近消费。
- **附魔单位（charm unit）** = 名额的最小消费粒度，按音符类型不同（见第 3 节）。
- **完成（结算）**：普通 = 命中；链接节点 = 头节点命中 / 段 CLEAR；连点 = 每个数字命中。
  每完成 **1 个附魔单位** → `ActiveSkillRuntime.OnPerCharmSuccess` 触发 **1 次**效果（Bomb 投弹 / Heal 回血 / Dispel 驱散）。

---

## 2. 皮肤状态映射（以 `skill_X` 皮肤为例）

| 音符状态 | 普通点击 | 跨轨 / 链接节点 | 小型点击 | 连点 / 数字 | 链接带 |
|---|---|---|---|---|---|
| 附魔（未命中） | `Note_Tap_skill_X` | `Note_Wide_skill_X` | `Note_Tap_Small_skill_X` | `Note_Repeat{剩余数}_skill_X` | `Note_Slide_Link_skill_X` |
| 完成 / 命中后 | `Note_Tap_Select_skill_X` | `Note_Wide_Select_skill_X` | `Note_Tap_Small_Select_skill_X` | 切 Select 系 / 恢复原色 | `Note_Slide_Judgment_skill_X` |

- **未附魔**的音符显示**基础皮肤**（无 `_skill_X` 后缀的 `Note_Tap` / `Note_Wide` / `Note_Tap_Small` / `Note_Repeat{数}` / `Note_Slide_Link` 等）。
- `Select` 系 = 该音符"已命中 / 已完成"的视觉态（对应 `NoteMover.HitCoroutine` 里切 Select 贴图的行为）。

---

## 3. 各音符类型的附魔规则（核心）

### 3.1 普通 / 小型 / 跨轨点击

- **1 个音符 = 1 附魔单位**。
- 命中结算触发 1 次效果；整体换附魔皮肤（`Note_Tap_skill_X` → 命中后 `Note_Tap_Select_skill_X`）。

### 3.2 链接音符（Linked / Hold，K 节点）

- **K 节点 = K 附魔单位**。
- **起始节点 = 1 单位**（点击即得分，独立一部分）。
- 第 `i ≥ 1` 节点 + 其**入边链接带** = 1 部分。
  - **链接带皮肤归属（视觉细化，2026-09-20 P4）**：一条链接带只要其**任一端节点被附魔**，就显示附魔皮肤（头节点附魔→点亮其出边首段；尾节点附魔→点亮其入边末段；中间节点附魔→入边+出边两段都亮）。这样任何被附魔的音节点，与它接触的链接带都会亮起，**消除"头节点附魔了但首段链接带还是原色"的视觉缺口**。未附魔的、两端都未附魔的链接带保持原色。
  - 严格"链接带归属下个音节"判定等价于：下个节点附魔→入边带换皮肤；P4 进一步让头节点也点亮出边，避免孤儿段。
- 每个节点完成（头节点命中 / 段 CLEAR）各触发 **1 次**效果。
- 示例：**三链接音符 = 可附魔 3 次、可完成 3 次附魔**。

### 3.3 连点音符（N 连）

- **N 个数字 = N 附魔单位**（整条最多占 N 名额）。
- **附魔 K 次（允许部分附魔）**：**前 K 个连续数字**附魔，每个命中触发 1 次效果。
- 第 **K+1 个数字起恢复原色**（显示基础皮肤 / 未附魔外观）。
- 部分附魔**占 K 名额**（未附魔的数字不占名额）。
- 等效理解：连点音符 = 多个音符叠加成的音符，对技能而言每完成一次附魔命中即完成一次附魔。

---

## 4. 实现状态与待办（代码层）

- [x] **链接音符计数**：已按每节点 1 单位（`HoldNote.TryCharmNode` 注册 `OnHoldCharmed(this,1)`），效果逐节点触发（`OnCharmNodeSuccess`）。
- [x] **换皮接线（P0 已实装，2026-09-20）**：`NoteSpriteLibrary.GetEnchantSkin(skillId,kind)` 按需 `Resources.Load` 皮肤贴图；`NoteSpawner.TintCharmed` 优先整张换技能皮肤（`NoteMover.ApplyEnchantSkin`），命中后切 Select 系；无皮肤集的技能（如炸弹雨）回退原 emission 染色。**P0 范围 = 美味牛角包 `skill_4_croissant_heal`**（30 张皮肤已就位 `Resources/EnchantSkins/skill_4_croissant_heal/`）。普通 / 小型 / 跨轨点击 + 链接节点（普通节点 `Note_Tap` / 跨轨节点 `Note_Wide`，含 Select 系）已换皮。
- [x] **附魔分配排序（P1 已实装，2026-09-20）**：`NoteSpawner.AssignPendingCharms` 把"场上未显现"与"尚未生成"的音符/节点合并成**统一按 hitTime 最近优先**的候选列表再分配，彻底修复"头部附魔、尾部漏附魔、名额被远处音符抢走"的排序漏洞（已生成但 hitTime 超出 leadTime 视界的链接尾部节点也纳入候选）。
- [x] **链接带染色（P3 已实装；P4 相邻段点亮；P5 连续区间整体点亮，2026-09-20）**：`HoldNote.RefreshCharmedBands()` 算出链上**首个/末个被附魔节点**，二者之间（含相邻外延）的所有链接带整段换 `Slide_Link_skill_X`；**充能液（select 覆盖带）同步换 `Slide_Link_Select_skill_X`**；节点完成（`ResolveCharmedNode` success）时把相邻两段标记"完成态"，覆盖优先用 `Slide_Judgment_skill_X`（`skill_4` 已补齐该皮肤）。无皮肤集技能（如炸弹雨）回退原带。
  - **规则（P5 用户拍板）**：段 `s`（连接节点 `s` 与 `s+1`）若 `(s+1 ≥ firstCharmed) 且 (s ≤ lastCharmed)` 即"落入连续附魔区间"→ 点亮。这样**头尾都被附魔时，中间即使有未拿到名额的节点，那段链接也照样亮**（消除内部暗缺口）；未拿到名额的节点只不触发效果、链接仍亮。已完成的段也保持皮肤（base=Link、overlay=Judgment），完成态不回退。
  - **实现关键**：`UpdateBands` 每帧把带子贴图重置回基础 `slideLinkTex`——换皮走**每段贴图覆盖表** `bandTexOverrides` / `bandOverlayTexOverrides`（null=基础）+ `bandCompletedOverlay`（bool，完成标记），由每帧刷新读取。皮肤贴图设 `wrapMode=Repeat` 与基础带一致（UV 沿带长平铺）。附魔/完成任一变化都调 `RefreshCharmedBands()` 重算整张覆盖表。
  - **进度线 marker**：也走皮肤覆盖 `progressTexOverride`（技能有 `Note_Slide_Judgment` 皮肤即换，每帧 `UpdateProgressMarker` 读取）。
  - **`skill_4` 的 `Slide_Judgment` 已补齐（31/31）**：按"判定条=纯色条=身色"规则（howl=BODY / bomb_rain=normal内芯 同先例），用**已验收的 004 本体填充实测色 `(98,188,159)`** 平涂基础 `Note_Slide_Judgment.png` 形状生成（alpha 逐像素保留，0 新造色）；源文件 `Tools/_gen_recolor/skill_4/`，已拷入 `Resources/EnchantSkins/skill_4_croissant_heal/`。
- [x] **连点名额拆分（P1 的一部分，2026-09-20）**：连点音符在分配时一次消费最多 `chainTapRequired` 个名额（`Note.enchantedHitCount = min(remaining, chainTapRequired)`）。
- [x] **连点逐数字皮肤 + 恢复（P2 已实装，2026-09-20）**：`NoteMover` 对附魔连点音符用覆盖层 quad 显示 `Repeat{chainTapRequired}_skill_X`，逐数字命中切 `Repeat{剩余}_skill_X`；`enchantedHitCount` 耗尽（`RestoreChainBase`）后隐藏皮肤 quad、复原原数字 TextMesh。
- [x] **连点效果触发（P2 的一部分，2026-09-20）**：从"末击 1 次"改为"逐附魔数字各 1 次" —— `Note.RegisterChainTapHit` 对每个附魔命中调 `ActiveSkillRuntime.OnCharmedNoteDigitHit`（触发 1 次效果），结算（`OnCharmedNoteResolved` 传 `triggerEffect=false`）不再重复计。

---

## 5. 与现有代码文件对应

| 职责 | 文件 |
|---|---|
| 附魔名额分配 / 候选展开 | `NoteSpawner.cs`（`RequestCharm` / `AssignPendingCharms` / `ReserveUpcomingCharms`） |
| 逐节点 / 逐音符结算 | `HoldNote.cs`（`TryCharmNode` / `ResolveCharmedNode`）、`Note.cs`（`RegisterChainTapHit` / `OnCharmedNoteResolved`） |
| 效果分发（Bomb / Heal / Dispel） | `ActiveSkillRuntime.cs`（`OnPerCharmSuccess` / `OnCharmNodeSuccess` / `OnCharmedNoteResolved`） |
| 命中切 Select 贴图 | `NoteMover.cs`（`HitCoroutine` / `ApplySelectTexture`） |
| 皮肤资产（31 张 / 皮肤生成器 + 锚点） | `RECOLOR_RULES.md` |

---

## 6. 铁律

- 附魔单位计数必须与"完成次数"一一对应：每完成 1 单位 = 1 次效果。
- 连点音符"恢复原色"= 切回**基础皮肤**（未附魔外观），不是另一种附魔色。
- 链接带染色粒度 = **连续附魔区间整体点亮**（首/末被附魔节点之间的所有链接带整段染，含未拿到名额的中间节点所在段），不逐段孤立判定。
- 数值 / 计数向上取整为整数（沿用项目全局规则）。
