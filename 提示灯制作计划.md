# 判定线提示灯（Lane Indicator）制作计划

> 文档用途：把提示灯迭代的完整上下文明文存档，防止制作/修改过程中失忆，或换机（主机/副机 WorkBuddy 交替）时对方能直接接管。
> 状态：方案已对齐，处于「待用户说『执行』再动手」阶段（verify-then-execute 铁律）。
> 最后更新：2026-09-24（副机）

---

## 0. 范围与无关项
- **本计划只覆盖**：判定线可见性 + 提示灯（LaneIndicator）视觉迭代 + 光效。
- **不在本计划**：连点音符（ChainTap/Repeat）新版造型与附魔换色 —— 已明确移交**主机 WorkBuddy** 处理，副机不碰、不同步，避免冲突。
- **轨道黑分隔线**（用户曾提的“细黑线隔开音轨”）：用户已决定**搁置**，等 1+2 完成后再议，本计划第 4 节仅为占位备忘。

---

## 1. 参考效果（来自用户截图，需对齐的视觉目标）
- **判定线提示灯（效果图）形态**：赛道边缘的**柔光胶囊灯条**——核心亮白/偏粉、向外粉红渐变、带辉光溢出（辉光由 Bloom 实现）。
- **用户最新口径（2026-09-24 调整）**：
  1. 现提示灯颜色**错误（蓝色）**，需要是**黑色/深灰**；且**各音轨提示灯之间要有连贯性，不能有间隙**。
  2. 提示灯**模型感太重（Cube 有体积）**，需要**降低厚度**，倾向换成贴地、像画在地面上的形态；是否附手绘贴图待定（倾向是，为了 COTL 手绘味）。
  3. 先做“底子”（颜色黑 + 连贯 + 薄），**光效是最后叠加的一步**。
- 判定线本体（LeftHitLine/RightHitLine）按用户要求：**Scene 编辑态可见，Play 运行态隐藏**（提示灯已作视觉参照，判定线不必画出来）。

---

## 2. 现状实测（代码/场景已核实，非凭记忆）
### 2.1 判定线 LeftHitLine / RightHitLine
- 场景对象：Cube 拉成的细条，scale 约 `0.1 × 0.05 × 6.5`，沿 Z 横跨；带 MeshRenderer + **BoxCollider**，Layer 0。
- 材质：`M_ArenaRed.mat`（截图里那条蓝线）。
- **安全性**：全工程**无任何脚本按名引用**这两个对象；触摸判定走 `TouchInputManager` 射线只打 Layer 6 的 TouchZone，与它的 BoxCollider 无关。**隐藏/禁用不影响输入判定、不影响 judgeLineX 逻辑坐标**。
- 注意区分：**BattleCenterLine（红蓝中线粉杠）不是判定线**，它驱动 `_CenterLineX` 红蓝地面分界 —— 本计划**不动**它。

### 2.2 提示灯 LaneIndicator（共 8 个，左右各 4 轨）
- 对象命名：`LeftBand_Indicator_Lane0~3`、`RightBand_Indicator_Lane0~3`，挂 `LaneIndicator` tag。
- 组件：Cube（MeshFilter）+ MeshRenderer + LaneIndicator(MonoBehaviour) + BoxCollider。
- **实测 Transform（已逐项读取 SampleScene.unity）**：

| 对象 | 位置(x,y,z) | 缩放(x,y,z) |
|---|---|---|
| Right Lane0 | (6, 0.06, -2.55) | 0.12 × 0.04 × 1.55 |
| Right Lane1 | (6, 0.06, -0.85) | 0.12 × 0.04 × 1.55 |
| Right Lane2 | (6, 0.06,  0.85) | 0.12 × 0.04 × 1.55 |
| Right Lane3 | (6, 0.06,  2.55) | 0.12 × 0.04 × 1.55 |
| Left  Lane0 | (-6, 0.06, -2.55) | 0.12 × 0.04 × 1.55 |
| Left  Lane1 | (-6, 0.06, -0.85) | 0.12 × 0.04 × 1.55 |
| Left  Lane2 | (-6, 0.06,  0.85) | 0.12 × 0.04 × 1.55 |
| Left  Lane3 | (-6, 0.06,  2.55) | 0.12 × 0.04 × 1.55 |

- 轨道间距 = 1.7，灯长 = 1.55 → **每两轨之间留 0.15 缝隙**（即用户看到的间隙）。
- 厚度 y=0.04（Cube 顶面到 0.08）+ 浮在地面上方 → 体积感来源。
- 当前材质：idle `guid 3e0a4bc8...`（亮蓝）、active `guid c3dbf631...`；`Flash/Hold/SetSleep` 接口 + 弹跳（`popScale`）逻辑完整，功能已 OK，**只换皮**。
- `BattleVisualsController` 通过 `indicators[]` 调 `Flash/Hold/SetSleep`，**不改**。

### 2.3 用户手绘贴图 tishideng.png（已交付并核验）
- 路径：`Assets/Art/UI/tishideng.png`
- 实测：256×152，RGBA 8bit。
- 渐变：左缘 alpha 228 → 线性渐隐 → **x=137 处归 0**（有效占宽 53.5%，右半全透明填充）；RGB 恒定灰 113（无颜色，全交 tint）。
- 垂直：上下完全均匀（沿长度拉伸无形变）。
- 结论：可用，无需重画。shader 用 `_GradientEnd`(默认 0.535) 把有效渐变区拉满全宽；左缘 alpha 228 差一点满，tint 强度补齐。

---

## 3. 已锁定方案（用户拍板部分）
### 3.1 判定线 Play 隐藏
- 新建 `PlayModeHide.cs`：`Awake()` 中 `if (Application.isPlaying) gameObject.SetActive(false);`
- 挂 `LeftHitLine` / `RightHitLine`；编辑态永远可见，进 Play 即隐藏，退出 Play Unity 自动还原（可逆、零风险）。BoxCollider 一并失活。

### 3.2 提示灯底子三改（先修底子，纯场景/材质改动，零代码）
1. **颜色 → 黑**：idle 主色改深灰黑，建议 `(0.06, 0.06, 0.07)`（接近黑但留一丝明度，免得跟地面阴影糊在一起）。顺手 **Cast Shadows off**（黑条不应投影）。
2. **连贯 → 每根 z 从 1.55 拉到 1.7**：填满轨道间距，8 根首尾相接成整条；位置坐标一个不动，保持 8 个独立对象（按轨闪烁逻辑不动）。
3. **厚度 → 换平躺 Quad（推荐，弃 Cube 压薄）**：零厚度、像画在地面上的漆线，彻底去模型感。

### 3.3 光效（最后叠加，前两步稳定后再做）
- 提示灯复用 `tishideng.png`（单侧渐变灰白）当形状剖面。
- URP Unlit 自定义 shader（单文件）：采样贴图 alpha 当 mask；`_Glow` 标量 0→1 在 `Update` 里 lerp 平滑过渡：
  - idle `_Glow=0` → 深灰不发光（普通 Alpha 材质，不是 Additive）；
  - 按下 `_Glow=1` → `_GlowColor` × `_GlowIntensity`(≈3) HDR，吃 M1 已开的 **Bloom** 出单向光晕。
- 参数（Inspector 全可调）：`_BaseColor` / `_GlowColor` / `_GlowIntensity` / `_Softness` / `_GradientEnd`(默认0.535) / `_Pulse`(可选呼吸，先 0)。
- 弹跳反馈：改**亮度过冲**（按下瞬间 `_Glow` 冲到 ~1.3 再回落 1.0），不依赖几何缩放。
- `Flash/Hold/SetSleep` 接口不动、`BattleVisualsController` 零改动、renderQueue 3050 保留。
- **红蓝分色（左红右蓝）后置**，shader 先统一一个暖粉白色，待用户要求再做。

### 3.4 1:1 尺寸基准（贴图 256×152 对应）
- 灯条 Quad：X 宽 **0.8m** ↔ 贴图 256px → **1px = 0.003125m**（有效渐变 ≈0.43m）；Z 向均匀拉伸，长度默认 **1.55**（同现判定线，可随时改）。
- 此基准为起点，Inspector 里 Quad scale 随时可调，贴图不变即跟着缩放，调形状直观。

---

## 4. 待用户拍板的开放点（执行前需确认）
1. **厚度方案**：Cube 压薄到 0.01（侧面仍露一丝） vs **换平躺 Quad（推荐，零厚度贴地画感）**。
2. **手绘纹理**：idle 黑条先**纯色**跑起来看效果，还是用户直接**画一张暗色斑驳条**贴上去（COTL 手绘味）。倾向先纯色，不够味再补图，避免过度制作。
3. **flash 按下材质过渡**：先改暖白色顶一下，还是干脆等光效一步到位（不单独改 activeMaterial）。

---

## 5. 执行时动的文件清单（待“执行”）
| 动作 | 文件 | 说明 |
|---|---|---|
| 新建 | `Assets/Scripts/PlayModeHide.cs` | 判定线 Play 隐藏 |
| 新建 | `Assets/Shaders/LaneIndicatorGlow.shader`（或 Assets/Art/...） | 提示灯发光单文件 URP Unlit |
| 改 | `Assets/Scripts/LaneIndicator.cs` | 双材质切换 → 驱动单材质 `_Glow`；弹跳改亮度过冲；接口不动 |
| 改 | `SampleScene.unity` | 8 个指示灯 Cube→Quad + 换材质 + z scale 1.7 + CastShadows off；判定线两对象挂 PlayModeHide |
| 引用 | `Assets/Art/UI/tishideng.png` | 发光形状剖面（已存在） |

---

## 6. 环境注意（换机/失忆时务必看）
- 本机 Bash 的 GitBuddy shim 曾损坏（`/d/` 挂载失败、coreutils 缺失），文件操作应走 Glob/Read/Write/Edit，像素级验证用托管 Python 标准库手写 PNG 解码（无 Pillow）。
- Git-LFS：WorkBuddy 自带 git 缺 LFS；提交用 UGit 客户端自带 git：`export PATH="/c/Users/Administrator/AppData/Local/UGit/app-5.53.0/resources/app/git/mingw64/bin:$PATH"`，或直接在 UGit 客户端提交。
- 提交纪律：连点音符（NoteMover.cs 半成品）属主机范围，**本计划不提交它**；本次若提交只提提示灯相关文件。

---

## 7. 收尾流程（所有制作验收完毕后执行）
1. **逻辑说明留档**：所有提示灯相关逻辑（判定线隐藏、提示灯底子参数、光效 shader 行为、`Flash/Hold/SetSleep` 接口契约、红蓝分色等）整理成一份简明说明文档并入工程（如 `Assets/Docs/提示灯逻辑说明.md` 或追加进本计划），供后续维护与换机接管查阅。
2. **清理制作期间备份/临时文件**：删除本次迭代产生的所有备份、临时、调试用文件（如 `.bak` / `_Old` / `Temp` / 调试快照 / 未纳入最终交付的草稿材质与贴图），保持工程干净无冗余。
3. **作废本计划文档**：上述留档完成、备份清理后，删除本文档 `提示灯制作计划.md` 本身（其使命是“防失忆接管”，验收交付后由第 1 步的逻辑说明文档接替）。

> 执行第 7 节前必须先确认：① 用户在 Unity 内完成视觉验收；② 相关改动已提交（或明确决定不提交）。清理动作不可逆，按“先确认后删除”铁律执行。
