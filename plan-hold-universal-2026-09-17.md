# 链接（按住）音符换皮 + 保底进度线 + 通用命中/Miss反馈 方案

> 状态：**F1~F4 已落地（代码已改，待 Unity 验证，2026-09-17 15:00）**。备份 Backup/2026-09-17-hold-f1-f4/（HoldNote/NoteMover/Note.cs/NoteSpawner）。
> - F1：带子 UV 改每段 0→1 整段拉伸（Wrap=Clamp，删 bandTileWorldLen）；带端内缩到节点外沿（bandEndInsetRatio 默认 1）；带子 renderQueue=2998 压在节点(3000)下。
> - F2：节点从黑圆柱换成平躺贴图 quad（普通 Note_Tap/Select、跨轨 Note_Wide/Select，1:1 尺寸），命中切 Select；ResetAllToBlack 改为重置 base 贴图+白 tint（不再发黑）。
> - F3（U1）：命中反馈改为 Update 驱动——不停顿继续前进、命中瞬间放大峰值(hitDecelScale=1.3)、越判定线缩小到 hitShrinkMin(0.75)+渐隐(hitFadeDist=1u 后销毁)，删旧 HitCoroutine；连点 E 也走此路径（chain 分支在反馈期让位普通移动）。
> - F4（U2）：Miss 改为继续前进+缩小渐隐（ApplyMissFeedback，missComplete 仍交 NoteSpawner 出 MISS），删旧 MissCoroutine；过粉杠硬切→渐显（NoteMover revealFadeDur=0.2 乘命中/Miss 系数；HoldNote 链接带 bandRevealFadeDur=0.2 乘越线消失系数）。
> 待 Unity 验证项：①带子渐变是否每段完整、接缝被节点盖住；②带端内缩量(bandEndInsetRatio)是否贴合节点；③节点贴图朝向/尺寸是否与带子协调；④命中/Miss 前进+渐隐手感；⑤过粉杠渐显时长；⑥链接带渐显。
> 本轮拍板：① H4 smoothstep ribbon **采纳**；② 加减速定案（减速段变大+切Select，加速段回常速，**全程不超正常速度**）；③ Miss 距离先按 **1u**，进游戏再调；④ 带子贴图由用户绘制（规格见 §5）；⑤ 充能发光 / 半透表现**后置**，本轮不做。
> 已执行代码块（HoldNote.cs，备份 Backup/2026-09-17-hold-ribbon/）：
> - 连接线从「16 圆柱细分」整体替换为**单连续 ribbon 网格**（H4 去锯齿），贴图 slideLink / slideLinkSelect 沿带长平铺（UV.tile = bandTileWorldLen）。
> - 越过判定线用 **smoothstep 软边**逐顶点 alpha 消失（bandFadeWidth，无放大变白，落实 H1）。
> - 已完成段切 Select 贴图（UpdateBandFill，按 completedSegments）。
> - **进度线 marker**（slideJudgment 贴图）置于当前进度点，保底债务 bandBreakDebt 时相对判定线回退（H2 线部分）。
> - 漏击：整条 α=0.75 + 越线软边消失（替代原整条统一缩小）。
> - 节点仍用旧圆柱（H3 节点换皮后置）；U1/U2（通用命中/Miss 对其他音符类型）尚未做。
> 前置：plan-repeat-note-2026-09-17.md（连点）已全部完成并验证。

## 0. 名词
- 链接音符 = 现有 HoldNote（点击后持续按住，跨轨时需拖动，直到结束音节）。
- 进度线 = 连接带上"已完成/未完成"的分界标记（视觉线，不是新判定器）。
- 通用反馈 = 所有音符类型共用的命中/Miss 消失表现（普点/小圈/跨轨/连点/链接）。

## 1. 保底进度线模型（第二轮拍板，用户已修正表现位置）
相对视角：连接带固定、判定线扫过。**进度线位置用"相对判定线的距离"表达**：

| 状态 | 进度线相对位置 | 说明 |
|------|--------------|------|
| ① 正常跟随 | 判定线 **前 0.5u**（固定超前） | 按住正确时进度线保持超前半个单位 |
| ② 保底计时 | 相对**往判定线移动**（差距缩小） | 拖错位置 → 前线后退；复用 breakTimer |
| ③ 保底耗尽 | **越过判定线** → 触发 Miss | 进度线消失、进度不再增长、整条 α=0.75 |
| ④ 恢复 | 重新**逐渐回移到 +0.5u** | 拖回正确位置 → 前线回追 |

- 实现：债务模型 `前线偏移 = 0.5u − debt`；debt 随 breakTimer 增长（增长率 = 0.5u / breakThreshold，保证越线瞬间恰好保底耗尽）；拖对时 debt 按恢复速率衰减。不加新计时器。
- **无警告色**：保底只靠"进度线相对移动"表达，不带警示色；带子颜色用既定四色（完成绿 / 未完成粉 / 进度线蓝 / 判定线红，见用户截图）。
- 参数全部 Inspector 可调：frontOffset=0.5、breakThreshold（已有 0.2s）、恢复速率。

## 2. 通用命中反馈（方案 A：逐对象 alpha，用户已拍板）
**命中（普点/小圈/跨轨/连点 CLEAR/技能清屏）**：
1. 不停顿，继续前进；
2. **只有"变大"那段减速**：减速段缩放变大 + 切 Select（完成贴图），峰值默认 scale≈1.3（可调）；
3. 随后加速回到正常速度，同时**变小**（缩幅很小，到完全消失仅缩 ~25%，即 endpoint scale≈0.75）+ 渐隐；
4. 越过判定线移动约 **1u** 后彻底消失销毁。

- **链接（按住）音符例外**：带子有"充能/填充"表现 → 命中完成**不放大**（不切 Select 整条换皮，改用充能进度 fill）。整体越过判定线后的通用消失仍套用 25% 缩小 + 渐隐（套整个带子组）。
- 不做真渐变遮罩（B 方案否决）；"遮罩"等价实现为按越线距离算 alpha。
- 需统一入口：普点/小圈/跨轨/连点 CLEAR/技能清屏全部切换到这套（链接完成段走 H 系列）。
- 波及：HitCoroutine（NoteMover）、CompleteChainTap 兼容。

**Miss（所有音符）**：
1. 继续前进（匀速）；
2. 变小（缩 ~25%）+ 渐隐，直到销毁（距离默认 **1u**，可调）。
- 现状 MissCoroutine 是原地缩小淡出 → 改为前进中缩小淡出。

## 3. 链接（按住）音符专项
- **命中持续完成**：进度线推进，越过判定线的带段持续消失（α 渐隐、不变形、不放大——去掉现有 ×1.6 放大变白）。
- **Miss/断连**：整条先降到 **α=0.75**（表达已无法完成），进度线消失、进度冻结，越线段照常走通用消失。
- **角度**：渐隐沿带弧长进行，端点始终吸附节点；消失期无自身位移/形状变化；**防锯齿方案已定 = (ii) 单连续 ribbon 网格 + smoothstep 软边**（见 H4）。
- **显现**：过粉杠硬切 → 渐显（α 0→1，默认 0.2s，可调）。通用改动 2。
- **关键坑**：渐显系数 × 消失系数相乘（两个独立 alpha 通道），避免"刚渐显就命中渐隐"打架；连点数字、段片、三件套走同一入口。

## 4. 分块排期（每块独立备份、可独立回退）
- **U1 通用命中反馈**：统一入口 + 各音符类型接入（先普点/小圈/跨轨/连点，链接完成段随后）。
- **U2 通用 Miss 反馈 + 过粉杠渐显**：MissCoroutine 改前进缩小淡出；SetAlpha 双系数。
- **H1 链接带持续消失改造**：去放大变白 → 沿弧长 α 渐隐（smoothstep 软边，无放大）。
- **H2 保底进度线**：前线渲染（单线 marker 相对判定线移动）+ 债务模型 + Miss α=0.75；颜色用既定四色、无警告色。
- **H3 链接节点换皮**：节点贴图/Select（贴图清单待 H 链接线做法敲定后用户提供）。
- **H4 防锯齿（已定方案 (ii)）**：单条连续 ribbon 网格（每节点间一段）+ 沿带长 UV 的 smoothstep 软边 alpha（过渡宽度 w 可调，与网格分辨率无关）；带子充能 fill 用 `_FillFront` 在已消耗区 lerp 基色→完成色。整链一或多个 ribbon，几何每帧按节点重算；角度自由。

## 5. 带子贴图规格（用户绘制，本轮唯一需要的贴图）
**只需 1 张：链接带 tile（横向长条，一节一格，左右无缝平铺）。** 节点（起/终点）先用现有 Note_Tap 占位，之后 H3 再定。

- **尺寸建议**：宽（沿带长方向）256px = 一个音节长度；高（带宽）96~128px（可按最终画面比例再调）。
- **格式**：PNG 带透明通道；带子本体（暗红芯 + 粉边高光，按你截图的风格）不透明，周围透明。
- **左右必须无缝**：贴图以 Repeat 方式沿带长平铺（UV.x 每前进一个音节走一格），两端图案要能拼上——最简单做法是左右两端用均匀的纯色/对称纹理，别在端头画断口或独立图案。
- **禁止画任何沿长度的明暗/alpha 渐变**：smoothstep 软边、渐显、充能 fill、消失渐隐全部由运行时控制（位置随音符动态移动，画死在贴图里位置就对不上了）。
- 上下边缘的描边/高光可以画（沿长方向均匀即可）。
- 导入设置：Wrap Mode = **Repeat**，其余同现有换皮贴图（透明 + 不生成 mip 亦可按现有约定）。

## 6. 第五轮：Unity 验证反馈修复迭代（F1~F4 已执行 2026-09-17 15:00）

> 用户跑游戏后反馈 4 个问题，逐条定位根因如下，修复拆成 F1~F4 四块，每块独立备份可回退。

### F1 带子贴图映射修复（对应问题 1 + 2）
**根因（问题1）**：我把 Note_Slide_Link 当"无缝 tile"用 Repeat 沿带长平铺（`uvx = accum / bandTileWorldLen`），但你画的是**一整段完整带子**（带首尾造型）→ 平铺后首尾图案反复出现，形成截图里的连续 X/菱形怪状。
**根因（问题2）**：带端锚定在节点**中心**（`nodeTransforms[s].position`），段与段的 UV 又各自从头重算，导致带子插进节点里、接缝图案错位；另外带子与节点的遮挡关系未处理。

**修法**：
1. UV 改为**每段 0→1 整段拉伸**：段起点 UV.x=0、段终点 UV.x=1，一整张贴图刚好铺满一段（两个节点之间）。去掉 `bandTileWorldLen` 平铺参数，贴图 Wrap 改 **Clamp**。
2. 带端吸附节点**边缘**：起点 = 节点中心 + dir×(noteRadius×endInsetRatio)，终点同理（endInsetRatio 默认 1.0，Inspector 可调），带子刚好从节点边缘出来，不再插进节点中心。
3. 遮挡关系：带子 renderQueue 压到节点之下（或带子 y 略降），保证节点圆盘盖在带子接头上，衔接视觉干净。
4. 若你其实想要"整条链接（首节点→末节点）一张图拉满"，改一个开关即可（`stretchMode: PerSegment / WholeChain`），默认 PerSegment——**待你确认**。

### F2 链接节点换皮（对应问题 3，即 H3 占位版）
**根因**：H3 一直后置，节点还是旧黑圆柱（截图里的黑圆盘）。
**修法**：先把节点换成**Note_Tap 贴图 quad**（复用 NoteMover 换皮思路：URP Unlit Transparent + MakeTapMaterial 显式混合），按 1:1 尺寸平躺；命中节点切 Note_Tap_Select。等你后续画好专属节点贴图，只换 `NoteSpriteLibrary` 字段引用即可，几何/材质逻辑不动。

### F3 U1 通用命中反馈接入（对应问题 4 前半）
**根因**：U1 尚未实现（排期在 H 验证之后），目前只有 NoteMover 原有 HitCoroutine（变大停顿闪黑）。
**修法（方案 A，已拍板）**：普点 / 小圈 / 跨轨 / 连点 CLEAR 统一切到新命中流程——
- 不停顿继续前进；只有"变大切 Select"段减速（峰值 scale≈1.3，Inspector 可调）；
- 随后加速回常速（全程不超正常速度）+ 缩小到 ≈0.75；
- 越过判定线后按距离渐隐（约 1u 彻底消失销毁）。
- 链接音符例外：不放大不切整条 Select，走带子 fill/软边（已实现），只套用越线渐隐。

### F4 U2 通用 Miss + 过粉杠渐显（对应问题 4 后半）
**根因**：U2 尚未实现，Miss 仍是原地缩小淡出；过粉杠仍是硬切 `enabled=true`。
**修法（已拍板）**：
- Miss：所有音符改"继续前进（匀速）+ 缩至 ≈0.75 + 渐隐"，1u 后销毁；
- 链接 Miss：整条先 α=0.75（已实现）+ 越线软边消失（已实现），本轮补齐与其他音符的一致性；
- 过粉杠渐显：显现 α 0→1 默认 0.2s（可调），**渐显系数 × 消失系数相乘**，避免打架；连点数字、链接带、节点走同一入口。

### 执行顺序与决策点（第五轮拍板结果，2026-09-17 14:26）
- **① stretchMode = PerSegment（每段一张 0→1，已定）**：用户贴图内部自带渐变（暗红芯→浅边），整条拉满会把渐变摊薄→否决 WholeChain。每两个音节之间完整呈现一次贴图渐变，段间接缝由节点圆盘遮盖。不做开关，直接删 bandTileWorldLen 平铺。
- **② 节点贴图 = 现有两套（已定）**：普通节点 Note_Tap / Note_Tap_Select，跨轨节点 Note_Wide / Note_Wide_Select，不新画。F2 把节点从黑圆柱换成贴图 quad（1:1 尺寸、平躺、命中切 Select），复用 NoteMover 换皮材质管线。
- **③ 遮挡关系（已定）**：带子层级压到节点下方，节点盖住接头。
- **衔接细节**：带端吸附节点边缘 = 带子从节点贴图外沿处开始/结束（endInsetRatio Inspector 可调），渐变图案不被节点压掉一截；露缝/重叠进游戏调参数。
- **顺序**：F1（带子映射+衔接）→ F2（节点换皮）→ F3（U1）→ F4（U2）。F1/F2 只动 HoldNote.cs + NoteSpriteLibrary；F3/F4 动 NoteMover.cs / Note.cs / NoteSpawner.cs，各自独立备份。

## 7. 第六轮 G 迭代：G1~G5 + 通用命中重设计（2026-09-17 15:35 **已执行**）

> F1~F4 落地后用户跑游戏又提 4 个新问题 + 通用命中节奏调整。逐条定位根因，拆 G1~G5，通用命中改回「停顿+恢复前进」。纯讨论，未动代码。

### G1 逐顶点渐显（修问题1「整根显示」）
**根因**：`UpdateBandVisibility` 段级显隐——`IsBeyondLine(节点s)||IsBeyondLine(节点s+1)` 任一端过粉杠整段 mesh 瞬间全显；`bandRevealAlpha` 是全局计时（所有段共用一个 0→1）。
**修法**：渐显改**逐顶点沿带长 smoothstep**，与消失软边完全对称——带子从粉杠位置起逐渐显现，永远只显示「已越杠」部分，不再整根砸出。
- 参数 `bandRevealWidth`（默认 0.5，可调）；删除全局 `bandRevealStart/bandRevealAlpha` 计时，改顶点世界 X 相对 `revealX` 的 smoothstep。

### G2 消失过渡（修问题2「没有过渡」）
**根因**：`bandFadeWidth=0.3` 太窄≈硬切；越线只 alpha 渐隐、无缩小，和通用命中不一致。
**修法**：`bandFadeWidth` 0.3→**0.8**（可调）；越线部分按距离轻微缩小带宽（下限 `bandShrinkMin=0.75`），对齐通用「缩小+渐隐 1u」。

### G3 进度线重做（修问题3「方块+终点挤压」）
**根因**：marker 用 1×1 方块 quad ×uniform scale(0.6)，贴图纵横比没管→方块；到末尾 `f` 被 Clamp01 钉终点、与节点/判定线叠挤压。
**修法（用户已定）**：
1. 进度线 quad **直接按 slideJudgment 贴图原始尺寸 1:1**（不再算纵横比/参数），每帧强制 localScale 防父级缩放干扰。
2. **完成覆盖**：新增一张「select 覆盖带」——用 `slideLinkSelect`，跟随进度线从链头逐渐覆盖到进度点（进度点之前=Select，之后=未完成 slideLink），形成「进度感」。实现：每段在进度点之前的部分切 Select 质感，当前进行段以进度点做平滑分界（复用现有逐顶点 alpha/uv 机制，可加一层覆盖 mesh 或每段按覆盖比例切 uv）。
3. 到达末段尽头 / 收尾阶段：进度线 **0.2s 淡出隐藏**，不再停在终点与节点叠挤。

### G4 衔接修复（修问题4「与音符衔接」）
**根因**：带端内缩用常数 `noteRadius×endInsetRatio` 沿带方向缩，普通/跨轨节点实际半宽差一倍，斜入带留缝或插进节点；带子与节点同 rideY 有 z-fighting 闪烁。
**修法**：带端内缩按**所接节点贴图实际半宽（沿带方向投影）**计算，普通/跨轨各自正确；带子 y 压低 0.02 防 z-fight；`bandEndInsetRatio` 保留微调。**用户授权**：我先调一版，不行用户上手调并报参数（参数 Inspector 可调）。

### G5 UV 平铺 + 渐变另加（修截图1 贴图拉糊）
**根因**：跨轨长段用 0→1 整段拉伸，段越长纹理拉越糊、渐变全糊。
**修法（用户拍板：改平铺）**：UV 改 **Repeat 平铺**（Wrap 改回 Repeat），`uv.x = 实际带长 ÷ (带宽 × 贴图宽高比)`，纹理任何段不变形，渐变每「一格带宽」重复一次。
**渐变另加（用户问「能不能用别的方式加上去」）**：平铺会重复纹理自带渐变，为给长段一致的「段内渐变感」，**额外沿整条链长加一层连续顶点色渐变**（subtle，如头暗→尾亮或用户四色方向），不依赖纹理拉伸——`bandGradient`（默认开，强度可调）。用户进游戏看是否要关。

### 通用命中重设计（U1-v2，改回「停顿+恢复前进」）
**根因**：U1-v1「减速再加速」实际手感太快。
**修法（用户拍板）**：命中 → **停顿（切 Select 贴图 + 变大 ×1.4）** → 停顿结束 **恢复常速前进（缩小到 0.75 + 渐隐 1u）** 后销毁。
- 即旧 HitCoroutine 的「停顿+变大」保留，但停顿结束后**不再原地消失，而是恢复前进并缩小变透明**（约 1u 走完渐隐），放弃 v1 的连续减速/加速。
- 连点 CLEAR（E）、小圈、跨轨、普点统一走此路径；链接音符仍例外（不放大，走带子 fill/软边）。

### 执行顺序
G1（逐顶点渐显）→ G2（消失过渡）→ G3（进度线重做 + select 覆盖）→ G4（衔接再调）→ G5（UV 平铺 + 渐变）→ U1-v2（通用命中重设计）。每块独立备份可回退。

### 待确认 / 用户授权
- G4 我先调一版，不行用户上手调报参数（参数已 Inspector 化）。
- G5 渐变另加层默认开，用户进游戏决定是否关。
- G3 完成覆盖用「每段按进度点切 Select + 当前段平滑分界」实现，细节进游戏验证。
